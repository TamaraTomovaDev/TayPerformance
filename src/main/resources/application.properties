# Database Verbinding
spring.datasource.url=jdbc:postgresql://localhost:5432/tay_db
spring.datasource.username=postgres
spring.datasource.password=intec-123
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Instellingen
# 'update' zorgt dat Spring Boot de tabellen (zoals appointments en customers) automatisch aanmaakt
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Server poort (standaard 8080)
server.port=8080

